# Test Environment Configuration
NODE_ENV=test
PORT=0

# Database Configuration (will be overridden by testcontainers when available)
DB_DIALECT=sqlite
DB_HOST=localhost
DB_PORT=3306
DB_NAME=order_management_test
DB_USER=root
DB_PASSWORD=test-password

# Logging Configuration
LOG_LEVEL=error
LOG_FORMAT=dev

# Application Configuration
API_PREFIX=/api
SWAGGER_ENABLED=false

# Test Configuration
USE_TESTCONTAINERS=false
